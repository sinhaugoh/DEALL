import 'package:flutter/material.dart';

class RetailerItem extends StatelessWidget {
  const RetailerItem(int index, { Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}