import 'package:flutter/material.dart';

class RetailerSignUpPage extends StatelessWidget {
  const RetailerSignUpPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}